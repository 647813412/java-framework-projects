spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      routes:
        - id: wuoj-backend-user-service
          uri: lb://wuoj-backend-user-service
          predicates:
            - Path=/api/user/**
        - id: wuoj-backend-question-service
          uri: lb://wuoj-backend-question-service
          predicates:
            - Path=/api/question/**
        - id: wuoj-backend-judge-service
          uri: lb://wuoj-backend-judge-service
          predicates:
            - Path=/api/judge/**
  #?SpringBoot??????????web???Servlet ??  ?  Reactive ??
  application:
    name: yuoj-backend-gateway
  main:
    web-application-type: reactive
server:
  port: 8101
knife4j:
  gateway:
    # ? ????????gateway????
    enabled: true
    # ? ??????????????discover???????
    strategy: discover
    discover:
      # ? ????????discover??
      enabled: true
      # ? ??????????????Swagger2?????
      version: swagger2